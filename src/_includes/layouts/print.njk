{% extends "layouts/print-frame.njk" %}


{% block content %}

    {%- if pagedjsCLI != true %}
    <div class="front-cover bleed">
        <div class="full-page-image">
            {% image ["LTLM45-2022-cover-front-", locale, "-v20220209a.jpg"] | join %}
        </div>
    </div>

    </div>
    <div class="front-collage bleed">
        {% image "front-photos-bleed.jpg" %}
    </div>
    <div class="opening-page">
        {% image "Calligraphy/zen-circle.jpg" %}
    </div>
    {%- endif %}

    {%- for article in articles -%}
        <article>
            {% set artCall = articleCalligraphies(article.fileSlug) %}
            {%- if artCall.hasCalligraphy %}
                <h1 id="{{ article.data.title | slugify }}" class="chapter calligraphyTitle">
                {% image artCall.calligraphyPath, "articleCalligraphy" %}</h1>
            {%- else %}
                <h1 id="{{ article.data.title | slugify }}" class="chapter">{{ article.data.title }}</h1>
            {%- endif %}
                
            <h2 class="author">{{ article.data.author }}</h2>
            {%- if pathPrefix != "" %}
                {{ article.data.content | safe | replace("/media/", pathPrefix+"/media/") }}
            {%- else %}
                {{ article.data.content | safe }}
            {%- endif %}
            <div class="article-end"></div>
        </article>
    {%- endfor -%}

    <div id="table-of-contents">
        <h1>{{ i18n.toc }}</h1>
        <ul class="toc">
            {%- for article in articles -%}
                <li><a href="{{ article.data.title | slugify }}">
                    <span class="toc-title">{{ article.data.title }}</span><br/>
                    <span class="toc-author">{{ article.data.author }}</span>
                </a></li>
            {%- endfor -%}
        </ul>
    </div>

    {%- if pagedjsCLI != true %}
    <div class="back-collage bleed">
        {% image "back-photos-bleed.jpg" %}
    </div>
    <div class="back-cover bleed">
        <div class="full-page-image">
                {% image ["LTLM45-2022-cover-back-", locale, "-v20220209a.jpg"] | join %}
        </div>
    </div>
    {%- endif %}

    {# {% include "partials/testing.njk" %} #}
{% endblock %}
